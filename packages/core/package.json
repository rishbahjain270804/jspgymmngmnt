{
  "name": "@oan/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Business rules shared by api, web, desktop and mobile. Nothing here may import a framework.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
